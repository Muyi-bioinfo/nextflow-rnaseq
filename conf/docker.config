// ============================================================
// Docker 执行配置
// ============================================================

docker {
    enabled = true
    runOptions = '-u $(id -u):$(id -g)'
}
